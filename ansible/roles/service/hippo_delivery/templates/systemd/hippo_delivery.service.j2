[Unit]
Description=Hippo CMS Server
After=network.target

[Service]
Type=forking

Restart=on-failure
ExecStart=/home/{{ hippo_delivery.user }}/tomcat/bin/startup_wrapper.sh
ExecStop=/home/{{ hippo_delivery.user }}s/tomcat/bin/shutdown_wrapper.sh

User={{ hippo_delivery.user }}
Group={{ hippo_delivery.user }}

[Install]
WantedBy=multi-user.target
